#include <stdio.h>
#include <main.h>

void printing(char *s);
int main()
{
	char s[] = "_putchar";
	return(0);
}
void printing(char*s);
{
	int i=0;
	while (s[i] != '\n')
	{
		putchar(s[i]);
		i++;
	}
}
