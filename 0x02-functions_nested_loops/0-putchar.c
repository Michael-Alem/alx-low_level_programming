#include "main.h"
#include <string.h>
#include <stdio.h>

void printing(char *s);
int main()
{
	char c[] = "_putchar";
	return(0);
}
void printing(char*s);
{
	int i=0;
	while (s[i] != '\n' && s[i] != '\0')
	{
		putchar(s[i]);
		i++;
	}
}
