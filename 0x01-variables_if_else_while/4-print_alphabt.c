#include <stdio.h>
/**
 * main-Main function
 * Description -Lower case alphabet letters
 * Return: void
 */
int main(void)
{
	char c;
	for (c = 'a' ; c <= 'z' ; ++c)
		putchar(c);
	putchar('\n')
		return (0);
}
