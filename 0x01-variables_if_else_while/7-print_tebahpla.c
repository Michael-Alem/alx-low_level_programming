#include <stdio.h>
/**
 * main - main function
 * Description - prints the task phrase
 * Return: Return void
 */
int main(void)
{
	char c;

	for (c = 'z' ; c >= 'a' ; --c)
		putchar(C);
			putchar('\n');
			return(0)
}
