#include <stdio.h>
/**
 * main - main function
 * Description - prints the task phrase
 * Return: Return void
 */
int main(void)
{
	int c;

	for (c = 0 ; c <= 9 ; ++c)
		printf("%d", c);
	printf('\n');
		return (0);
}
